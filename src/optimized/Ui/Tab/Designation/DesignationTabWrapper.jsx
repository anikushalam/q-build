import style from "../../UiTab.module.css";

const DesignationTabWrapper = ({ children }) => {
  return <div className={style.designation_tab_wrapper}>{children}</div>;
};

export default DesignationTabWrapper;
